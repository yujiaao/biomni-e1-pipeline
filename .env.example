# 运行所需环境变量（本文件不写真实值，已加入 .gitignore）
# 复制为 .env 并填入你自己的 key，或直接 export 到 shell。

# DeepSeek API key（用于 LLM 提取/聚合，二选一配置）
DEEPSEEK_API_KEY=

# HuggingFace 镜像（国内访问 sentence-transformers 模型用）
HF_ENDPOINT=https://hf-mirror.com
HF_HUB_ENDPOINT=https://hf-mirror.com
